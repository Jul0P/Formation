<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - Pokémon Battle</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: system-ui, -apple-system, sans-serif;
        background: hsl(0 0% 98%);
        color: hsl(0 0% 10%);
        line-height: 1.6;
      }
      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 1rem;
      }
      header {
        border-bottom: 1px solid hsl(0 0% 90%);
        padding: 2rem 0 1rem;
        margin-bottom: 2rem;
      }
      h1 {
        font-size: 1.875rem;
        font-weight: 600;
      }
      nav {
        display: flex;
        gap: 1.5rem;
        margin-top: 1rem;
        flex-wrap: wrap;
      }
      nav a {
        color: hsl(0 0% 40%);
        text-decoration: none;
        font-size: 0.875rem;
        transition: color 0.2s;
      }
      nav a:hover {
        color: hsl(0 0% 10%);
      }
      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.5rem 1rem;
        background: hsl(0 0% 10%);
        color: white;
        text-decoration: none;
        border-radius: 0.375rem;
        border: none;
        font-size: 0.875rem;
        cursor: pointer;
        transition: background 0.2s;
      }
      .btn:hover {
        background: hsl(0 0% 20%);
      }
      .card {
        background: white;
        border: 1px solid hsl(0 0% 90%);
        border-radius: 0.5rem;
        padding: 1.5rem;
        margin-bottom: 1rem;
      }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 1rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Pokémon Battle</h1>
        <nav>
          <a href="/">Accueil</a>
          <a href="/trainers">Dresseurs</a>
          <a href="/pokemon">Pokémon</a>
          <a href="/attacks">Attaques</a>
          <a href="/battles/new">Combats</a>
        </nav>
      </header>
      <main><%- body %></main>
    </div>
  </body>
</html>
